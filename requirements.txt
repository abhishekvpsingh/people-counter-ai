# Core CV + models
ultralytics==8.0.197
opencv-python==4.11.0.86
mediapipe==0.10.21
numpy>=1.23,<2
protobuf>=3.20,<5
pillow>=10,<11

# PyTorch CPU build (works for macOS/Intel/Apple Silicon via Rosetta)
# If you already have torch installed, you can remove these two lines.
torch==2.2.2
torchvision==0.17.2

# Optional UI
streamlit==1.40.1
tqdm>=4.66,<5
